# Legacy / archived tree

Everything in this folder was moved here on **2026-04-09** when the repo was trimmed to the **static `cv-v2.html` stack** (root-level `css/`, `js/`, `img/`, `assets/`, Azure bundle script).

- **Next.js app:** `app/`, `components/`, `public/`, `package.json`, configs, `lib/`, `types/`, etc.
- **Older static / experiments:** `cv.html`, `cv-gsap.html`, dashboards, `learn-scrollstory/`, root `cv.css` / `styles.css`, `gsap-cv-replace.js`, `script.js`, `dist/`.
- **Extra JS (pre–cv-v2):** `old-js/` — former root `js/main.js`, `sections.js`, `scroll-controller.js`, `components.js`.
- **MCP test script:** `test-mcp.ts`（原 `scripts/test-mcp.ts`）
- **GitHub Pages workflow (deployed `dist/`):** see `github-pages-deploy.yml` (no longer active under `.github/workflows/`).

To work on the current site, open **`cv-v2.html`** at the repository root and see `docs/SECTION_CODE_GUIDE.md`.
